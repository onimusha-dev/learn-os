# PCB (Process Descriptor):
 - **Process Controll Block:** data sctacture that keeps track of processes 
 - **Address space:** the memory that the process can access (the address space) and is a part of the process
 - **CPU state (machine state):** 
 1. **program counter | instruction pointer:** this tells us which instruction in the process is currently exicuting
 2. **Stack pointer | Frame pointers:** used to manage the function parameters, local variables and return addresses
 3. 